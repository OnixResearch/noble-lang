(contract 1 composed-increments
  (input (x I64))
  (output (y I64))
  (program [
    (block (I64) (I64) [ 1 + ])
    (block (I64) (I64) [ 1 + ])
    compose run ])
  (requires true)
  (ensures (eq (out y) (add (add (in x) 1) 1))))
